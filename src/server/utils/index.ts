//§—————————————————————————————————————————————————————————————————————————————————————————————§//
//§                                     NO BARREL FILE HERE                                     §//
///
//# Util functions often import random stuff from across the server code. I don't thing that
//# is a bad thing, but it has a huge potential for creating import problems when exported
//# through a barell file. Since i expect this folder to get rather large as times go on,
//# instead of dealing with all these problems and then using targeted imports anyway, I hereby
//# declare this index file to stay empty, until i find a better approach.
//#
//# When creating folders/files here, be mindful of what you export and from where.
//#
//? While writing the note above, i realized that 'utils' probably isn't a good name for this
//? place anyway, since it does not really contain any general utilities but big chunks of
//? self-contained code. This might be refactored in the future.
///
//§—————————————————————————————————————————————————————————————————————————————————————————————§//
